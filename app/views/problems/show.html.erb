<% @title = "问题：#{@problem.title}"%>

<div class="box" style="background-color:#E9F4E9;padding:15px;">
  <h3><%= @problem.title %></h3>
  <pre><%= @problem.intro %></pre>
  <div style="float:right">
    <%= raw(interest_to(@problem)) %>
  </div>
</div>

<div style="float:right;margin-bottom:20px;">
  <%= render :partial => '/public/share'%>
</div>

<div class="box">
  案例：
  <% unless @kases.empty?%>
    <span>（<%= link_to "全部#{@problem.kases.count}", "#{problem_path(@problem)}/kases"%> | <%= link_to "添加案例", "#{problem_path(@problem)}/kases/new"%> | <%= link_to "查看案例分布", position_problem_path(@problem)%>）</span>
    <div style="margin:20px 0 0 20px;">
      <% @kases.each do |k|%>
        <%= link_to (image_tag k.photo.url(:_100x75)), "#{problem_path(@problem)}/kases/#{k.id}", :style=>'margin:5px;', :title=>k.intro%>
      <% end %>
    </div>
  <% else %>
    <p>
      <span style="margin-left:30px;">该问题还没有案例，您可以 <%= link_to "添加案例", "#{problem_path(@problem)}/kases/new"%></span>
    </p>
  <% end %>
</div>

留言板：
<%= render :partial => '/comments/board'%>    
<%= render :partial => '/comments/form' ,:locals => {:commentable => @problem}%>

<% content_for :sidebar do %>
  <div class="box">
    <%= form_tag new_problem_path, :method => :get do%>
      <%= text_field_tag 'keywords', @keywords,:style => 'font-size:15px;width:235px;padding:5px'%>
      <%= submit_tag '添加问题',:style=>'font-size:14px;line-height:20px;float:right;' %>
    <% end %>
  </div>
  
  <div class="box">
    <%= link_to "查看全部",followers_problem_path(@problem),:class => "see_all_link" if @following_users[8]%>
    <h5><%= "#{@problem.follows.size}人关心该问题："%></h5>
    <%= render :partial => '/users/user',:collection => @following_users[0..7] %>
  </div>
<% end %>