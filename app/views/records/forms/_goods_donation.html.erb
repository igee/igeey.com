        <div class="impact">
          <%= raw(@calling.present? ? "#{@calling.title} #{form.hidden_field(:title,:value => @calling.title)}" : "#{form.label :title,'行动标题：'} #{form.text_field(:title, :size => 20 ,:placeholder => '做的是什么行动？')}")%>
        </div>
        <div style="line-height:30px">
          <%= form.label :goods,'捐赠数量：'%> <%= form.text_field :goods, :size => 3 ,:placeholder => "",:value => (@record.goods || (@plan.present? && @plan.goods)) || nil%>
          　 <%= form.label :done_at,'完成日期：'%><%= form.text_field :done_at, :size => 8,:class => 'with_calendar',:placeholder => "",:value => (@record.done_at || (@plan.present? && @plan.plan_at)) || nil%><br/>
        </div>
        <br/>
        <h5>行动详情：</h5>
        <%= form.text_area :detail, :style => "width:460px;height:80px"%>
        <br/>
        <%= form.fields_for :photos do |photo|%>
          <h5>行动照片：<%= photo.file_field :photo,:style => "background:none;border:none;font-size:13px" %></h5>
        <% end %>