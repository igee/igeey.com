<div class="venue">
  <%= link_to image_tag(venue.cover.url(:_48x48),:class => "venue_thumb_cover",:alt => venue.name),venue,:title => venue.name %>
  <br/>
  <span><%= link_to short_text(venue.name,4),user_path(venue) if defined?(with_name)%></span>
</div>