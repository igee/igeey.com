<% @title = "话题"%>
  
  <div class="box" >
    <h4>全部话题 <span><%= link_to '添加话题',new_tag_path() if logged_in? && current_user.is_admin?%></span></h4>
    <%= render :partial => "/public/tag",:collection => @tags %>
    <%= will_paginate @tags %>
  </div>
  
<% content_for :sidebar do %>  
<% end %>