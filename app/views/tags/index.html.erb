<% @title = "标签"%>
  
  <div class="box" style="width:240px;word-wrap:break-word;word-break:normal;overflow:hidden;">
    <h4>全部标签 <span><%= link_to '添加标签',new_tag_path() if logged_in? && current_user.is_admin?%></span></h4>
    
    <%= raw tag_list_for(Tag) %>
  </div>  