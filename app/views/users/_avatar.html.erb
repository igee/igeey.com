<% user = avatar %>
<div class='avatar' user_id="<%=user.id%>">
  <%= link_to (image_tag user.avatar.url,:class => "user_avatar",:alt => user.login),user %>
  <br/>
  <span><%= link_to user.login,user_path(user) if defined?(with_name)%></span>
</div>
